// next.config.js (ES module style)
import path from 'path';

const nextConfig = {
    reactStrictMode: true,
    pageExtensions: ['tsx', 'ts', 'js', 'jsx'],
    // add other config if needed
};

export default nextConfig;
